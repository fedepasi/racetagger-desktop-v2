#!/usr/bin/env bash
# DEPRECATED (2026-06-08) — the Backlog Autopilot is RETIRED, replaced by the
# Claude issue-triage routine. The backlog-autopilot-{sweep,implement} workflows
# are disabled (cron commented + `if: false`), so dispatching them is a no-op.
# See docs/ROUTINES_REGISTRY.md and docs/ISSUE_TRIAGE_ROUTINE.md.

echo "Backlog Autopilot is DEPRECATED (2026-06-08) — replaced by the Claude issue-triage routine."
echo "The backlog-autopilot workflows are disabled (if:false). See docs/ROUTINES_REGISTRY.md."
exit 1
