# Daily GitHub Issues Triage — racetagger-clean — 2026-04-28

**Eseguito:** 2026-04-28T09:15Z (automatico, scheduled task)
**Cutoff finestra:** 2026-04-27T07:15Z → 2026-04-28T09:15Z (~26h)
**Repo:** [fedepasi/racetagger-desktop-v2](https://github.com/fedepasi/racetagger-desktop-v2)
**Issue analizzate:** 2 (#125, #124)
**Branch creati:** 0 (vedi sezione "Limitazioni di esecuzione")

---

## Limitazioni di esecuzione

> ⚠️ Step 4 e Step 5 (creazione branch, commit, push, commento, label) **non sono stati completati automaticamente**. La sandbox in cui gira questo task non ha credenziali GitHub di scrittura: `git push` fallisce con `could not read Username for 'https://github.com'`. La sola lettura via API pubblica funziona ed è ciò che ha permesso il fetch delle issue.
>
> **Cosa serve da te (manuale, ~5 minuti):**
> 1. Per ciascuna delle proposte in `.cowork/proposals/issue-124.md` e `.cowork/proposals/issue-125.md`:
>    - `git checkout -b bugfix/issue-125-totali-non-tornano` (e analoga per #124)
>    - committare il file di proposta
>    - `git push -u origin <branch>`
>    - commentare sull'issue: "🤖 Cowork triage: branch `bugfix/issue-N-...` created with proposed approach. See `.cowork/proposals/issue-N.md`."
>    - applicare label `cowork-triaged` (creandola se manca, colore `#0891b2`)
> 2. Committare anche questo report e i due file di proposta su `main` (oppure spostare le proposte solo sui rispettivi branch e tenere solo il report su `main`).
>
> In alternativa, fornisci alla sandbox un PAT (Personal Access Token) GitHub con scope `repo` salvato in un secret store accessibile dallo skill, e i prossimi run della scheduled task saranno completamente autonomi.

---

## Issue #125 — [BUG] I totali non tornano

**Link:** https://github.com/fedepasi/racetagger-desktop-v2/issues/125
**Autore:** fedepasi (`fede.pasi+2@gmail.com`) · **Aperta:** 2026-04-28T07:27:14Z · **Aggiornata:** 2026-04-28T07:27:14Z
**Label:** bug, user-feedback

**Problema segnalato**
La pagina risultati riporta 19 totali / 15 matched / 4 no-match. Filtrando "No Match" la lista mostra solo 2 immagini. Nessun commento, nessun PR linkato, nessuna repro step ulteriore oltre alla descrizione e al diagnostic report Supabase.

**Impatto**
- Severità: `medio`
- Utenti probabilmente colpiti: tutti gli utenti con almeno un'immagine in cui Gemini rileva un veicolo ma non il pettorale (frequente: 5–15% in moto/ciclismo).
- Effetto: erosione fiducia ("se i totali non tornano, gli altri dati saranno ok?"). Nessun impatto su token/conversion.

**Soluzione proposta**
Il filtro `no-match` in `racetagger-clean/renderer/js/log-visualizer.js` (line 1263–1265) considera no-match SOLO le immagini con `analysis` vuoto/assente. Il contatore (`updateStatistics`, line 1375–1386) calcola `noMatch = total - matched - needsReview`, dove `matched` esige che esista almeno un veicolo con `raceNumber` valido (non vuoto, non `'N/A'`). Risultato: una foto con veicolo rilevato ma `raceNumber` mancante è "no-match" per il contatore ma non per il filtro. Fix: estrarre i predicate (`isMatched`, `isNoMatch`, `isNeedsReview`, `hasCorrection`) e usarli in entrambe le posizioni. Dettagli e patch sketch in `.cowork/proposals/issue-125.md`.

Confidenza causa radice: **alta** — il bug è leggibile direttamente dal codice senza bisogno di repro.

**Stima sforzo**
- Dimensione: `XS` (<1h)
- Rischio regressione: `basso`
- Test richiesti: manual on real data (l'esecuzione utente nel diagnostic report) + opzionale unit sui predicate.

**Priorità consigliata**
`P1 — questa settimana`. Bug visibile, fix banale, alta visibilità → da rilasciare nella patch v1.1.5.

**Branch:** non creato in automatico (vedi limitazioni). Nome suggerito: `bugfix/issue-125-totali-non-tornano`.

---

## Issue #124 — [BUG] Verifica lentezza allo start

**Link:** https://github.com/fedepasi/racetagger-desktop-v2/issues/124
**Autore:** fedepasi (`fede.pasi+2@gmail.com`) · **Aperta:** 2026-04-28T06:59:16Z · **Aggiornata:** 2026-04-28T06:59:16Z
**Label:** bug, user-feedback

**Problema segnalato**
L'analisi ci ha messo "parecchio tempo" a partire, poi è proseguita correttamente. Mancano: misura precisa del delay, numero di immagini in cartella, dimensione del preset. Il diagnostic report Supabase (link nell'issue, valido 7 giorni) contiene 1000 righe di main process log e dovrebbe consentire di misurare il delta.

**Impatto**
- Severità: `medio`
- Utenti probabilmente colpiti: tutti, ma principalmente cartelle grandi (1000+ file), preset grandi (1000+ partecipanti) o reti lente.
- Effetto: first-impression peggiore, churn risk indiretto, possibile chiusura/riapertura app con perdita di token pre-autorizzati.

**Soluzione proposta**
Indagine necessaria sul diagnostic log. Senza la misura del delta `t_click → t_first_image`, restano ipotesi. In ordine di priorità:

1. **Roundtrip Supabase serialli prima di `processBatch()`** — `handleUnifiedImageProcessing` in `src/main.ts` (line 1259) esegue in sequenza: `getSportCategoryIdByName` (DB), `createExecutionOnline` (DB), `readdir`+stat parallelo, eventuale `getParticipantPresetByIdSupabase` (DB, solo se preset arriva vuoto — Issue #104 guardrail), import dinamico smart-matcher. Tutto questo blocca prima che la UI veda `unified-processing-started`.
2. **Token pre-authorization lato renderer** — la RPC `pre_authorize_tokens()` viene invocata prima dell'IPC e ha latenza variabile. Su rete lenta può aggiungere 1–3s.
3. **File stat collection su Windows con antivirus** — `Promise.all(files.map(stat))` su 2500+ RAW con Windows Defender attivo può richiedere secondi.
4. **`SmartMatcher.startSession()` + dynamic import alla prima esecuzione** — modulo non ancora cached al primo run dopo l'apertura dell'app.

Fix proposto: parallelizzare le query DB, anticipare l'`unified-processing-started`, eventualmente pre-warmare la pre-authorization al folder select. Pseudocodice in `.cowork/proposals/issue-124.md`.

Confidenza causa radice: **media** — molto probabilmente è un mix di (1) e (2). Bisogna confermare con il log.

**Stima sforzo**
- Dimensione: `M` (3–8h, di cui 1–2h di investigazione)
- Rischio regressione: `medio` (la sequenza esecuzione → token → tracking è critica)
- Test richiesti: manual on real data (cartella ~2500 RAW, preset ~500 partecipanti, misurare delta), integration test su Issue #104 guardrail (preset vuoto), regression test pipeline token.

**Priorità consigliata**
`P1 — questa settimana`. First-impression issue. Risolvibile in metà giornata se il log conferma una delle prime due ipotesi.

**Branch:** non creato in automatico (vedi limitazioni). Nome suggerito: `bugfix/issue-124-lentezza-start-analisi`.

---

## Riepilogo

| Issue | Severità | Effort | Risk | Priorità | Branch creato? |
|-------|----------|--------|------|----------|---------------|
| #125  | medio    | XS     | basso | P1      | ❌ — credenziali GitHub mancanti, fare manualmente |
| #124  | medio    | M      | medio | P1      | ❌ — credenziali GitHub mancanti, fare manualmente |

Entrambe sarebbero state idonee al criterio di creazione branch (P0/P1 + (XS|S|M) + (basso|medio)). Le proposte tecniche complete sono in `.cowork/proposals/issue-{124,125}.md`.

## File generati da questo run

- `.cowork/reports/2026-04-28-triage.md` — questo report
- `.cowork/proposals/issue-125.md` — analisi e patch sketch per #125
- `.cowork/proposals/issue-124.md` — analisi, ipotesi e patch sketch per #124
